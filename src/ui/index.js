/**
 * UI Adapter Layer
 * 
 * Change the import path below to swap UI implementations.
 * Available options:
 * - './implementations/native' - Pure HTML/CSS (no dependencies)
 * - './implementations/mantine' - Mantine UI (for BlockNote compatibility)
 * - './implementations/shadcn' - shadcn/ui (requires setup)
 * - './implementations/mui' - Material-UI (requires setup)
 */

// Change this line to swap UI implementations
export * from './implementations/native';

// Examples:
// export * from './implementations/mantine';
// export * from './implementations/shadcn';
// export * from './implementations/mui';
